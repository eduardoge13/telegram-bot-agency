---
phase: 02-whatsapp-bot-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - whatsapp-bot/app/__init__.py
  - whatsapp-bot/app/main.py
  - whatsapp-bot/app/config.py
  - whatsapp-bot/app/businesses.py
  - whatsapp-bot/app/session_store.py
  - whatsapp-bot/app/gemini_client.py
  - whatsapp-bot/app/handlers/__init__.py
  - whatsapp-bot/app/handlers/base.py
  - whatsapp-bot/app/handlers/qa.py
  - whatsapp-bot/app/sheets/__init__.py
  - whatsapp-bot/app/providers/__init__.py
  - whatsapp-bot/requirements.txt
  - whatsapp-bot/pytest.ini
  - whatsapp-bot/tests/__init__.py
  - whatsapp-bot/tests/conftest.py
  - whatsapp-bot/tests/test_webhook.py
  - whatsapp-bot/tests/test_config.py
  - whatsapp-bot/tests/test_session_store.py
  - whatsapp-bot/tests/test_health.py
  - whatsapp-bot/.env.example
autonomous: true
requirements: [PLAT-01, PLAT-03, PLAT-04, BOT-01, BOT-02]

must_haves:
  truths:
    - "Twilio webhook receives POST at /webhook and validates signature correctly"
    - "Invalid Twilio signature returns 403"
    - "Business registry resolves correct config by Twilio number"
    - "Adding a third business config entry routes correctly without code change"
    - "Two sessions with different (business_id, phone) keys are isolated"
    - "Session expires after inactivity timeout"
    - "Gemini generates Spanish response using business system prompt"
    - "/health returns 200"
  artifacts:
    - path: "whatsapp-bot/app/main.py"
      provides: "FastAPI app with /webhook and /health endpoints, Twilio signature validation"
      exports: ["app"]
    - path: "whatsapp-bot/app/businesses.py"
      provides: "BusinessConfig dataclass and BUSINESSES registry dict keyed on Twilio number"
      exports: ["BusinessConfig", "BUSINESSES"]
    - path: "whatsapp-bot/app/session_store.py"
      provides: "SessionStore with TTL-based (business_id, phone) keyed sessions"
      exports: ["SessionStore", "ConversationSession"]
    - path: "whatsapp-bot/app/config.py"
      provides: "Settings via pydantic-settings loading from env vars"
      exports: ["Settings", "get_settings"]
    - path: "whatsapp-bot/app/gemini_client.py"
      provides: "Gemini client wrapper creating chat objects with system_instruction"
      exports: ["GeminiClient"]
    - path: "whatsapp-bot/app/handlers/base.py"
      provides: "BaseHandler abstract class for handler pattern"
      exports: ["BaseHandler"]
  key_links:
    - from: "whatsapp-bot/app/main.py"
      to: "whatsapp-bot/app/businesses.py"
      via: "BUSINESSES[to_number] lookup in webhook handler"
      pattern: "BUSINESSES\\[.*to_number"
    - from: "whatsapp-bot/app/main.py"
      to: "whatsapp-bot/app/session_store.py"
      via: "session_store.get_or_create() in webhook handler"
      pattern: "session_store\\.get_or_create"
    - from: "whatsapp-bot/app/session_store.py"
      to: "whatsapp-bot/app/gemini_client.py"
      via: "GeminiClient creates chat objects for new sessions"
      pattern: "gemini_client"
---

<objective>
Build the WhatsApp bot service foundation: FastAPI app with Twilio webhook, Gemini AI integration, business registry, and session store with TTL isolation.

Purpose: Establish the core message receive-route-respond pipeline that all handlers will build on. After this plan, the bot can receive WhatsApp messages via Twilio, identify the business, maintain conversation context per customer, generate AI responses in Spanish via Gemini, and reply via TwiML.

Output: A working FastAPI service at `whatsapp-bot/` with tests, ready for handler implementation in Plan 02.
</objective>

<execution_context>
@/Users/eduardogaitan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardogaitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-whatsapp-bot-platform/02-RESEARCH.md
@.planning/phases/02-whatsapp-bot-platform/02-CONTEXT.md
@.planning/phases/01-site-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project scaffold, config, business registry, and session store with tests</name>
  <files>
    whatsapp-bot/requirements.txt,
    whatsapp-bot/pytest.ini,
    whatsapp-bot/.env.example,
    whatsapp-bot/app/__init__.py,
    whatsapp-bot/app/config.py,
    whatsapp-bot/app/businesses.py,
    whatsapp-bot/app/session_store.py,
    whatsapp-bot/app/gemini_client.py,
    whatsapp-bot/app/handlers/__init__.py,
    whatsapp-bot/app/handlers/base.py,
    whatsapp-bot/app/handlers/qa.py,
    whatsapp-bot/app/sheets/__init__.py,
    whatsapp-bot/app/providers/__init__.py,
    whatsapp-bot/tests/__init__.py,
    whatsapp-bot/tests/conftest.py,
    whatsapp-bot/tests/test_config.py,
    whatsapp-bot/tests/test_session_store.py
  </files>
  <behavior>
    - test_business_lookup: BUSINESSES["whatsapp:+15005550006"] returns BusinessConfig with business_id="puntoclave"
    - test_business_lookup_unknown: BUSINESSES.get("whatsapp:+19999999999") returns None
    - test_new_business_no_code_change: Adding a third entry to BUSINESSES dict makes it resolvable (validates PLAT-04)
    - test_session_isolation: Two sessions with keys ("puntoclave", "+521111") and ("travel", "+521111") are independent objects
    - test_session_expiry: Session created, then last_active set to past beyond timeout, get_or_create returns a fresh session
    - test_session_reuse: Session within TTL is reused (same object returned)
  </behavior>
  <action>
    1. Create `whatsapp-bot/` directory structure per the research architecture pattern.

    2. `requirements.txt`: fastapi>=0.110, uvicorn>=0.29, twilio>=9.0, google-genai>=1.0, google-api-python-client==2.134.0, google-auth>=2.0, httpx>=0.27, python-dotenv==1.0.1, pydantic>=2.0, pydantic-settings>=2.0, pytz==2024.1, pytest, pytest-asyncio.

    3. `pytest.ini`: testpaths=tests, asyncio_mode=auto.

    4. `.env.example`: TWILIO_AUTH_TOKEN, TWILIO_ACCOUNT_SID, GEMINI_API_KEY, GOOGLE_CREDENTIALS_JSON, AMADEUS_CLIENT_ID, AMADEUS_CLIENT_SECRET, AMADEUS_BASE_URL=https://test.api.amadeus.com, WEBHOOK_BASE_URL=https://bot.srv1175749.hstgr.cloud.

    5. `app/config.py`: Pydantic Settings class loading from .env: twilio_auth_token, twilio_account_sid, gemini_api_key, google_credentials_json, amadeus_client_id, amadeus_client_secret, amadeus_base_url (default test), webhook_base_url. Singleton `get_settings()`.

    6. `app/businesses.py`: BusinessConfig dataclass with fields: business_id, twilio_number, system_prompt, sheets_id (optional), sheets_range (default "Sheet1!A:D"), handlers (list of str), language (default "es"). BUSINESSES dict keyed on twilio_number string. Two entries per CONTEXT.md:
       - "whatsapp:+15005550006" (placeholder) for Punto Clave MX: handlers=["product", "order", "qa"], system_prompt in Spanish describing an e-commerce assistant that responds conversationally, mentions prices as $X,XXX MXN, proactively offers ordering.
       - "whatsapp:+15005550007" (placeholder) for travel agency: handlers=["flight", "qa"], sheets_id=None, system_prompt in Spanish describing a travel assistant that helps search flights.
       Note: Twilio numbers are placeholders — will be replaced with real numbers via env vars or config update.

    7. `app/gemini_client.py`: GeminiClient class wrapping `google.genai.Client`. Method `create_chat(system_prompt: str)` returns a chat object via `client.chats.create(model="gemini-2.0-flash", config=types.GenerateContentConfig(system_instruction=system_prompt, max_output_tokens=1024, temperature=0.7))`. Constructor takes api_key param.

    8. `app/session_store.py`: SessionStore class with `_sessions: dict[tuple, ConversationSession]`, `_gemini_client: GeminiClient`. ConversationSession dataclass: chat object, last_active float, state dict. `get_or_create(business_id, phone, system_prompt)` — checks TTL (SESSION_TIMEOUT_SECONDS=1800), creates new chat via GeminiClient if expired or missing. `cleanup_expired()` method to purge old sessions.

    9. `app/handlers/base.py`: Abstract BaseHandler with: `async def can_handle(self, message: str, state: dict) -> bool` and `async def handle(self, message: str, session: ConversationSession, business: BusinessConfig) -> str`.

    10. `app/handlers/qa.py`: QAHandler that always returns True for can_handle (fallback). handle() calls `await session.chat.send_message_async(message)` and returns `.text`.

    11. Create empty `__init__.py` files for all packages. Create `tests/conftest.py` with shared fixtures (mock settings, sample business configs).

    12. Write tests FIRST per TDD, then implement. For session_store tests, mock the GeminiClient to avoid real API calls.
  </action>
  <verify>
    <automated>cd whatsapp-bot && pip install -r requirements.txt -q && pytest tests/test_config.py tests/test_session_store.py -x -v</automated>
  </verify>
  <done>
    - BusinessConfig and BUSINESSES registry exist with 2 business entries
    - SessionStore isolates sessions by (business_id, phone) tuple
    - Sessions expire after TTL timeout
    - Adding a third business to BUSINESSES works without code changes
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FastAPI app with Twilio webhook, health endpoint, and signature validation</name>
  <files>
    whatsapp-bot/app/main.py,
    whatsapp-bot/tests/test_webhook.py,
    whatsapp-bot/tests/test_health.py
  </files>
  <behavior>
    - test_health: GET /health returns 200 with {"status": "ok"}
    - test_signature_valid: POST /webhook with valid Twilio signature returns 200 with TwiML XML
    - test_signature_invalid: POST /webhook with invalid signature returns 403
    - test_url_reconstruction: validate_twilio reconstructs HTTPS URL from x-forwarded-proto header
    - test_unknown_business: POST /webhook with valid signature but unknown To number returns TwiML with error message
  </behavior>
  <action>
    1. `app/main.py`: Create FastAPI app. Import BUSINESSES, SessionStore, QAHandler, Settings.

    2. Health endpoint: `GET /health` returns `{"status": "ok", "service": "whatsapp-bot"}`.

    3. Twilio signature validation as FastAPI dependency `validate_twilio(request: Request)`:
       - Reconstruct URL: use `WEBHOOK_BASE_URL` env var + request.url.path (more reliable than header reconstruction per research pitfall #1). Fallback to x-forwarded-proto + host if WEBHOOK_BASE_URL not set.
       - Parse form data, get X-Twilio-Signature header.
       - Use `twilio.request_validator.RequestValidator(settings.twilio_auth_token)` to validate.
       - Return 403 HTTPException if invalid.

    4. Webhook endpoint: `POST /webhook` with `Depends(validate_twilio)`:
       - Extract To (business number), From (customer), Body (message) from form data.
       - Look up business: `BUSINESSES.get(to_number)`. If not found, reply with "Lo siento, este servicio no esta disponible."
       - Get or create session via SessionStore.
       - For now, use QAHandler as fallback (dispatcher comes in Plan 02).
       - Build TwiML response using `MessagingResponse().message(reply_text)`.
       - Return `Response(content=str(resp), media_type="application/xml")`.

    5. App lifespan: Initialize SessionStore and Settings on startup. Store as app.state attributes.

    6. Write tests using FastAPI TestClient. Mock the Twilio RequestValidator to control signature validation. Mock GeminiClient to avoid real API calls. Test that:
       - /health returns 200
       - Valid signature processes message and returns TwiML
       - Invalid signature returns 403
       - Unknown To number returns graceful TwiML error
  </action>
  <verify>
    <automated>cd whatsapp-bot && pytest tests/test_webhook.py tests/test_health.py -x -v</automated>
  </verify>
  <done>
    - GET /health returns 200 with status ok
    - POST /webhook with valid Twilio signature returns TwiML XML response
    - POST /webhook with invalid signature returns 403
    - Unknown business number gets graceful error reply
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd whatsapp-bot && pytest tests/ -x -v
```
All tests pass: test_config, test_session_store, test_webhook, test_health.
</verification>

<success_criteria>
- whatsapp-bot/ project structure exists with all core modules
- FastAPI app starts and serves /health and /webhook endpoints
- Twilio signature validation works (valid accepted, invalid rejected)
- Business registry maps Twilio numbers to BusinessConfig
- Session store isolates conversations by (business_id, phone) with TTL expiry
- GeminiClient wrapper creates chat objects with system_instruction
- BaseHandler abstract class and QAHandler fallback exist
- All unit tests pass with `pytest tests/ -x -v`
</success_criteria>

<output>
After completion, create `.planning/phases/02-whatsapp-bot-platform/02-01-SUMMARY.md`
</output>
