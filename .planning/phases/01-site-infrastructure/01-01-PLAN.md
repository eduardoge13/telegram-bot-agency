---
phase: 01-site-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/lib/constants.ts
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/app/page.tsx
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/components/ProductCard.tsx
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/components/SpeiPaymentSection.tsx
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/next.config.ts
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/Dockerfile
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/docker-compose.yml
  - /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/.dockerignore
autonomous: true
requirements:
  - ECOM-01
  - ECOM-02
  - ECOM-03

must_haves:
  truths:
    - "All WhatsApp links on the site open a conversation with 5572408666"
    - "The site builds successfully with Next.js standalone output"
    - "A Docker container can be built and started from the Dockerfile, serving the site on port 3000"
    - "The SPEI/CLABE payment section is visible on the homepage"
    - "The site design looks premium and conversion-optimized for a Mexican tech e-commerce brand"
  artifacts:
    - path: "lib/constants.ts"
      provides: "Centralized WhatsApp number and CLABE constants"
      contains: "WHATSAPP_NUMBER"
    - path: "app/page.tsx"
      provides: "Homepage with design improvements and SPEI section"
      min_lines: 50
    - path: "components/SpeiPaymentSection.tsx"
      provides: "SPEI bank transfer trust section"
      min_lines: 15
    - path: "next.config.ts"
      provides: "Standalone output configuration"
      contains: "output.*standalone"
    - path: "Dockerfile"
      provides: "Multi-stage Next.js standalone build"
      contains: "node:22-alpine"
    - path: "docker-compose.yml"
      provides: "Service definition with restart policy"
      contains: "restart.*unless-stopped"
  key_links:
    - from: "app/page.tsx"
      to: "lib/constants.ts"
      via: "import WHATSAPP_NUMBER"
      pattern: "import.*WHATSAPP_NUMBER.*constants"
    - from: "components/ProductCard.tsx"
      to: "lib/constants.ts"
      via: "import WHATSAPP_NUMBER"
      pattern: "import.*WHATSAPP_NUMBER.*constants"
    - from: "app/page.tsx"
      to: "components/SpeiPaymentSection.tsx"
      via: "import and render SpeiPaymentSection"
      pattern: "SpeiPaymentSection"
    - from: "Dockerfile"
      to: ".next/standalone"
      via: "npm run build produces standalone output"
      pattern: "COPY.*standalone"
---

<objective>
Fix the Punto Clave MX e-commerce site code: replace the WhatsApp placeholder number, add SPEI bank transfer section, improve the design for conversion, configure Next.js standalone output, and create Docker deployment files.

Purpose: Prepare the site codebase so it is ready to deploy to the VPS in Plan 02. All code changes happen here; Plan 02 only handles server-side deployment.
Output: A complete, buildable, containerizable Next.js site with correct WhatsApp number, SPEI section, and professional design.
</objective>

<execution_context>
@/Users/eduardogaitan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/eduardogaitan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-site-infrastructure/01-CONTEXT.md
@.planning/phases/01-site-infrastructure/01-RESEARCH.md

@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/app/page.tsx
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/app/layout.tsx
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/components/ProductCard.tsx
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/lib/products.ts
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/data/products.json
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/types/product.ts
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/next.config.ts
@/Users/eduardogaitan/Documents/projects/spike-ecommerce-web/package.json

<interfaces>
<!-- The ecommerce site is a SEPARATE repo at /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/ -->
<!-- All file modifications happen in that repo, NOT in telegram-bot-agency -->

From app/page.tsx (line 6):
```typescript
const WHATSAPP_NUMBER = '5215512345678'; // MUST be replaced
```

From components/ProductCard.tsx (line 5):
```typescript
const WHATSAPP_NUMBER = '5215512345678'; // MUST be replaced
```

From next.config.ts:
```typescript
const nextConfig: NextConfig = {
  /* config options here */  // MUST add output: 'standalone'
};
```

From types/product.ts:
```typescript
export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;  // in MXN cents
  category: string;
  image: string;
  available: boolean;
}
```

From lib/products.ts:
```typescript
export function getAllProducts(): Product[]
export function getProductById(id: string): Product | undefined
export function getProductsByCategory(category: string): Product[]
```

Existing stack: Next.js 16.1.6, React 19.2.3, Tailwind CSS 4.x, Google Fonts (Syne + Outfit)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WhatsApp number, add constants, configure standalone output, and create Docker files</name>
  <files>
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/lib/constants.ts
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/app/page.tsx
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/components/ProductCard.tsx
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/next.config.ts
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/Dockerfile
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/docker-compose.yml
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/.dockerignore
  </files>
  <action>
    All work happens in the spike-ecommerce-web repo at /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/

    1. Create `lib/constants.ts` with:
       - `export const WHATSAPP_NUMBER = '5572408666';`
       - `export const SPEI_CLABE = '000000000000000000';` with a TODO comment: "Replace with client's real CLABE"

    2. In `app/page.tsx`:
       - Remove the hardcoded `const WHATSAPP_NUMBER = '5215512345678';` on line 6
       - Add `import { WHATSAPP_NUMBER } from '@/lib/constants';` at the top
       - Verify all `wa.me` links use the imported constant

    3. In `components/ProductCard.tsx`:
       - Remove the hardcoded `const WHATSAPP_NUMBER = '5215512345678';` on line 5
       - Add `import { WHATSAPP_NUMBER } from '@/lib/constants';`

    4. In `next.config.ts`:
       - Add `output: 'standalone'` to the NextConfig object

    5. Create `Dockerfile` — multi-stage build per research pattern:
       - Stage 1 (deps): `node:22-alpine`, copy package*.json, `npm ci --frozen-lockfile`
       - Stage 2 (builder): copy source + node_modules, `npm run build`
       - Stage 3 (runner): `node:22-alpine`, `NODE_ENV=production`, `PORT=3000`, `HOSTNAME=0.0.0.0`
       - Copy `.next/standalone`, `.next/static`, and `public` from builder
       - Expose 3000, CMD `["node", "server.js"]`

    6. Create `docker-compose.yml`:
       ```yaml
       services:
         ecommerce:
           build: .
           container_name: spike_ecommerce
           ports:
             - "3000:3000"
           restart: unless-stopped
           environment:
             - NODE_ENV=production
       ```

    7. Create `.dockerignore` with: node_modules, .next, .git, *.md, docs/

    IMPORTANT: Do NOT add any Stripe/Mercado Pago/cart/product-detail-page code — those are deferred to v2.
  </action>
  <verify>
    <automated>cd /Users/eduardogaitan/Documents/projects/spike-ecommerce-web && grep -r "5215512345678" app/ components/ lib/ | wc -l | xargs test 0 -eq && grep "output.*standalone" next.config.ts && grep "WHATSAPP_NUMBER" lib/constants.ts && grep "import.*constants" app/page.tsx && grep "import.*constants" components/ProductCard.tsx && test -f Dockerfile && test -f docker-compose.yml && npm run build</automated>
  </verify>
  <done>
    - Old WhatsApp number (5215512345678) appears nowhere in app/, components/, or lib/
    - Both page.tsx and ProductCard.tsx import WHATSAPP_NUMBER from lib/constants.ts
    - next.config.ts has output: 'standalone'
    - Dockerfile exists with multi-stage build
    - docker-compose.yml exists with restart: unless-stopped
    - `npm run build` succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add SPEI payment section and improve site design for conversion</name>
  <files>
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/components/SpeiPaymentSection.tsx
    /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/app/page.tsx
  </files>
  <action>
    All work happens in the spike-ecommerce-web repo at /Users/eduardogaitan/Documents/projects/spike-ecommerce-web/

    1. Create `components/SpeiPaymentSection.tsx`:
       - Import `SPEI_CLABE` from `@/lib/constants`
       - Display a "Pago por Transferencia Bancaria" section
       - Include an SPEI-style visual indicator (use a bank/transfer icon or styled badge — do NOT use external image URLs for the SPEI logo; create it with Tailwind/SVG)
       - Show the CLABE number prominently with a copy-to-clipboard button (navigator.clipboard.writeText)
       - Add brief Spanish instructions: "Realiza tu pago por transferencia SPEI y envIa tu comprobante por WhatsApp"
       - Style with Tailwind: trust-building colors (greens/blues), clear typography, card-style container
       - Mark component as 'use client' (needs clipboard API)

    2. Improve `app/page.tsx` design — act as a professional web designer:
       - Add the SpeiPaymentSection component after the "Como funciona" section or in a natural trust-building position
       - Consolidate product data: the homepage has hardcoded `slides` array AND there's `data/products.json` — use `data/products.json` via `getAllProducts()` from `lib/products.ts` to eliminate duplication. Transform product data to slide format if needed.
       - Integrate the unused `ProductCard` component from `components/ProductCard.tsx` — add a product grid section below the hero carousel showing all products
       - Improve visual hierarchy: ensure clear section separation, consistent spacing, strong CTAs
       - Improve selling points: add trust indicators (shipping info, warranty, authentic products badges)
       - Ensure responsive design: looks great on mobile (primary for Mexican market) and desktop
       - Fix any UX issues found (duplicated data, unused components, unclear CTAs)
       - All copy MUST be in Spanish
       - Use existing fonts: Syne for headings, Outfit for body text
       - Do NOT add cart functionality, product detail pages, or payment checkout — those are v2

    IMPORTANT: This is a Mexican tech e-commerce brand (Punto Clave MX). The design should convey:
    - Trust and legitimacy (authentic products)
    - Premium but accessible pricing
    - Easy WhatsApp-based ordering process
    - Multiple payment options (WhatsApp + SPEI transfer)
  </action>
  <verify>
    <automated>cd /Users/eduardogaitan/Documents/projects/spike-ecommerce-web && grep "SpeiPaymentSection" app/page.tsx && grep "SPEI_CLABE" components/SpeiPaymentSection.tsx && grep "getAllProducts\|products" app/page.tsx && npm run build</automated>
  </verify>
  <done>
    - SpeiPaymentSection.tsx exists and is rendered on the homepage
    - CLABE number is displayed with copy-to-clipboard functionality
    - Product data is consolidated (no duplicate hardcoded slides if products.json covers the same items)
    - ProductCard component is used in a product grid section
    - Site builds successfully with all design improvements
    - All text is in Spanish
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/eduardogaitan/Documents/projects/spike-ecommerce-web && npm run build` — succeeds with zero errors
2. `grep -r "5215512345678" app/ components/ lib/` — returns empty (old number fully replaced)
3. `grep "5572408666" lib/constants.ts` — constant is correct
4. `docker build -t spike-ecommerce-test .` — image builds successfully (optional, requires Docker locally)
5. Site renders correctly when run with `npm run dev` (visual check)
</verification>

<success_criteria>
- The old WhatsApp placeholder number appears nowhere in the codebase
- All WhatsApp links use the centralized constant pointing to 5572408666
- Next.js standalone build succeeds
- Dockerfile and docker-compose.yml are ready for VPS deployment
- SPEI payment section is visible on the homepage with CLABE and copy button
- Site design is professionally improved with trust indicators, product grid, and clear CTAs
- All UI text is in Spanish
</success_criteria>

<output>
After completion, create `.planning/phases/01-site-infrastructure/01-01-SUMMARY.md`
</output>
