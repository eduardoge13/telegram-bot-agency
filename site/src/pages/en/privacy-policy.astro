---
import LegalPage from '../../components/LegalPage.astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { localizedPaths, privacyPolicyContent } from '../../config/site';

const copy = privacyPolicyContent.en;
---

<BaseLayout
	locale="en"
	title={`Blue Sky Travel | ${copy.title}`}
	description={copy.description}
	path={localizedPaths.privacy.en}
	alternatePath={localizedPaths.privacy.es}
>
	<LegalPage
		locale="en"
		title={copy.title}
		intro={copy.intro}
		description={copy.description}
		contactCard={copy.contactCard}
		sections={copy.sections}
	/>
</BaseLayout>
